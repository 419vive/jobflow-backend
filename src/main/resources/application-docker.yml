spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/jobflow
    username: jobflow
    password: jobflow
  jpa:
    hibernate:
      ddl-auto: validate
  redis:
    host: redis
    port: 6379
